# Precision shaft — Ø24 × 90 mm

Reference: V-656 mechanical

## Archie's engineering plan (the spec)
Turned shaft to exact diameter and length — a single clean cylinder. STEP out.

## Execution (human-like app drive)
Prompt: model a precision shaft as a single cylinder, exactly 24 mm in diameter and 90 mm long

Render: pathtrace-gpu @ 128 spp (M4 Max GPU ray tracing)
Deliverable: glb + STEP (manufacturing) + STL + render PNG
