# Build artifacts produced by the OCCT WASM build — do NOT commit (large).
archdisc-occt.js
archdisc-occt.wasm
archdisc-occt.d.ts

# Build logs — large, machine-specific; do NOT commit.
build.log
min-build.log
