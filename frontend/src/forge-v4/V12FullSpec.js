// PUSH-24 — Frontend mirror of specs/mercedes-m120-v12-full.json so the
// bundle can read the spec at runtime without fs access.

import spec from '../../../specs/mercedes-m120-v12-full.json';

export const M120_FULL_SPEC = spec;
export default M120_FULL_SPEC;
