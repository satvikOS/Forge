# Reap receipt: the unifyfaces-pcurve-guard worktree

**Reclaimed 689 MB.** Removed through `git worktree remove`, never `rm -rf`.

    path      .claude/worktrees/unifyfix
    branch    fix/unifyfaces-pcurve-guard
    size      689 MB (475 MB checkout + build-verify/build-noguard/build-wide)

## The four preconditions, each checked rather than assumed

1. **The work provably exists elsewhere.** PR #63 merged into
   `claude/sacrosanct-execution-20260828` as `b872aff5`. Verified by reading the
   files back OUT of the remote ref, not by trusting the merge report:
   `git show origin/claude/...:forge-kernel/src/DirectEdit.cpp` contains the guard
   function (3 occurrences), and the gate script is present with its 9 marker lines.
2. **Tracked tree clean.** `git status --porcelain` showed nothing but the three
   untracked `build-*` directories, which are reproducible CMake output.
3. **No unique commits.** `git rev-list --count HEAD --not --remotes=origin` = 0.
4. **Not in use.** `lsof -nP +D` returned only this session's own shell and the
   pipeline reading it (`lsof`, `awk`, `sort`, `zsh`) -- no `forge_verify`, `cmake`,
   or python process holding the directory. The first check appeared to show `head`
   holding it; that was the `head -5` in the checking command itself.

`archdisc-Mech` is back to ZERO worktrees.

## Not reaped, and why

The 29 Tenure worktrees remain KEEP. Every single one is either dirty or holds
commits reachable from no remote -- there is no worktree there for which "already
pushed" is true. The 8232 MB of `node_modules` across them is regenerable via
`npm ci` and is the only safe reclaim, deliberately NOT taken: disk sits at 152Gi
free and disk pressure never converts uncertainty into deletion authority.
